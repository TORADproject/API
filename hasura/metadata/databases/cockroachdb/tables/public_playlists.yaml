table:
  name: playlists
  schema: public
array_relationships:
  - name: playlist_songs
    using:
      foreign_key_constraint_on:
        column: playlist_id
        table:
          name: playlist_songs
          schema: public
